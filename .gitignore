# Python
__pycache__/
*.py[cod]
*.pyo
venv/
.env
*.log
*.egg-info/
dist/
build/

# Node
node_modules/
frontend/dist/
frontend/.vite/

# macOS
.DS_Store

# Logs
backend/backend.log
frontend/frontend.log
*.log

# pytest cache
.pytest_cache/

# Runtime state (auto-generated by backend)
backend/data/history.db*
backend/data/baseline_cron.json

# Deployment secrets — SA JSON / API keys placed alongside handoff data
# (handoff/ is mounted into the container; this dir is intentionally NOT tracked)
handoff/_secrets/

# structured Kibana event logs
backend/logs/

# 驗證用截圖 / 瀏覽器暫存 / 本機工作目錄 — 永不入版控
/*.png
/*.zip
.playwright-mcp/
.claude/
# repo 根目錄 data/ 放本機憑證與工作檔 (含 SA JSON),嚴禁 git add
/data/
